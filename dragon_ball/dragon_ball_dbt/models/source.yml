sources:
  - name: raw
    description: file download from S3
    schema: raw
    tables:
      - name: student_performance
        description: "Student data from kaggle"
        meta:
          external_location: "read_csv('../data/raw/{name}.csv')"